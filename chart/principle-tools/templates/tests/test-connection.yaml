apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "principle-tools.fullname" . }}-test-connection"
  labels:
    {{- include "principle-tools.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "principle-tools.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
