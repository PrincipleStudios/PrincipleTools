apiVersion: v1
kind: Service
metadata:
  name: {{ include "principle-tools.fullname" . }}
  labels:
    {{- include "principle-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "principle-tools.selectorLabels" . | nindent 4 }}
